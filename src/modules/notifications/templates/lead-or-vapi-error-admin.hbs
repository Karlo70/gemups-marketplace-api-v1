<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lead Alert - Action Required</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'Inter', -apple-system, BlinkMacSystemFont, Roboto, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f5f7ff 0%, #f0f2fa 100%);
            color: #333;
            line-height: 1.6;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .email-container {
            max-width: 700px;
            width: 100%;
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 50px rgba(124, 58, 237, 0.15);
            border: 1px solid #eaeaea;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .email-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 60px rgba(124, 58, 237, 0.25);
        }

        .email-header {
            background: linear-gradient(120deg, #7c3aed 0%, #5b21b6 100%);
            color: white;
            padding: 35px 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .header-pattern {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0.1;
            background:
                radial-gradient(circle at 10% 20%, rgba(255, 255, 255, 0.8) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(255, 255, 255, 0.5) 0%, transparent 30%);
        }

        .logo-container {
            position: relative;
            z-index: 2;
        }

        .app-logo {
            width: 70px;
            height: 70px;
            background: white;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .app-logo i {
            color: #7c3aed;
            font-size: 32px;
        }

        .email-header h1 {
            font-size: 26px;
            font-weight: 700;
            margin: 10px 0 5px;
            letter-spacing: -0.5px;
        }

        .email-header p {
            opacity: 0.85;
            font-size: 16px;
            max-width: 500px;
            margin: 0 auto;
        }

        .status-badge {
            display: inline-block;
            padding: 8px 18px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
            margin-top: 20px;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(5px);
        }

        .email-body {
            padding: 40px 35px;
        }

        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #7c3aed;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }

        .section-title i {
            margin-right: 10px;
            background: #f0ebff;
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 25px 0;
        }

        .info-card {
            background: #f9fafb;
            border-radius: 14px;
            padding: 20px;
            border-left: 4px solid #7c3aed;
            transition: transform 0.3s ease;
        }

        .info-card:hover {
            transform: translateY(-3px);
        }

        .info-card h3 {
            font-size: 15px;
            font-weight: 600;
            color: #6b7280;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
        }

        .info-card h3 i {
            margin-right: 8px;
            color: #7c3aed;
        }

        .info-card .value {
            font-size: 18px;
            font-weight: 600;
            color: #1f2937;
        }

        .message-card {
            background: #f3f4f6;
            border-radius: 14px;
            padding: 20px;
            margin: 25px 0;
            border-left: 4px solid #8b5cf6;
        }

        .message-card p {
            line-height: 1.7;
            color: #4b5563;
        }

        .technical-section {
            background: #f8fafc;
            border-radius: 14px;
            padding: 25px;
            margin: 30px 0;
            border: 1px solid #e5e7eb;
        }

        .tech-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        .tech-item {
            display: flex;
        }

        .tech-item .label {
            font-weight: 600;
            color: #4b5563;
            width: 120px;
            flex-shrink: 0;
        }

        .tech-item .value {
            color: #1f2937;
            font-weight: 500;
        }

        .error-section {
            background: #fef2f2;
            border-radius: 14px;
            padding: 25px;
            margin: 30px 0;
            border-left: 4px solid #ef4444;
        }

        .error-title {
            color: #b91c1c;
            font-weight: 700;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }

        .error-title i {
            margin-right: 10px;
        }

        .error-content {
            background: white;
            border-radius: 10px;
            padding: 18px;
            font-family: monospace;
            font-size: 14px;
            color: #dc2626;
            line-height: 1.7;
            overflow-x: auto;
            margin-top: 15px;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
            margin: 35px 0 20px;
        }

        .btn {
            flex: 1;
            min-width: 200px;
            padding: 16px 25px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn i {
            margin-right: 10px;
            font-size: 18px;
        }

        .btn-primary {
            background: #7c3aed;
            color: white;
            box-shadow: 0 5px 15px rgba(124, 58, 237, 0.3);
        }

        .btn-primary:hover {
            background: #6d28d9;
            box-shadow: 0 7px 20px rgba(124, 58, 237, 0.4);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: white;
            color: #7c3aed;
            border: 2px solid #e0e7ff;
        }

        .btn-secondary:hover {
            background: #f5f3ff;
            transform: translateY(-2px);
        }

        .btn-error {
            background: #ef4444;
            color: white;
            box-shadow: 0 5px 15px rgba(239, 68, 68, 0.25);
        }

        .btn-error:hover {
            background: #dc2626;
            box-shadow: 0 7px 20px rgba(239, 68, 68, 0.35);
            transform: translateY(-2px);
        }

        .email-footer {
            background: #f9fafb;
            padding: 25px 30px;
            text-align: center;
            color: #6b7280;
            font-size: 14px;
            border-top: 1px solid #e5e7eb;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 25px;
            margin: 15px 0;
        }

        .footer-links a {
            color: #7c3aed;
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-links a:hover {
            color: #5b21b6;
            text-decoration: underline;
        }

        .copyright {
            margin-top: 15px;
            opacity: 0.8;
        }

        @media (max-width: 650px) {

            .email-header,
            .email-body,
            .email-footer {
                padding: 25px 20px;
            }

            .email-header h1 {
                font-size: 22px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn {
                min-width: 100%;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="email-header">
            <div class="header-pattern"></div>
            <div class="logo-container">
                <div class="app-logo">
                    <i class="fas fa-bullhorn"></i>
                </div>
                <h1>New Lead Requires Attention</h1>
                <p>Review the details and take appropriate action</p>
                <div class="status-badge">
                    <i class="fas fa-circle-exclamation"></i> ACTION REQUIRED
                </div>
            </div>
        </div>

        <div class="email-body">

            <!-- CRON JOB SHUTDOWN NOTICE -->
            <div class="error-section" style="margin-bottom: 30px;">
                <div class="error-title" style="color: #b91c1c;">
                    <i class="fas fa-stop-circle"></i> Cron Job Shutdown Notice
                </div>
                {{!-- <p style="color: #991b1b;">
                   <strong>Important:</strong> The scheduled cron job responsible for <b>sending or initiating {{channel}} notifications</b> has been <b>shut down</b>. Other automated functions and notifications, are still operating normally. Please investigate and restart the {{channel}} sending function of the cron job to resume {{channel}} delivery.
                </p> --}}
            </div>
            <!-- END CRON JOB SHUTDOWN NOTICE -->

            <div class="section-title">
                <i class="fas fa-user"></i> Lead Information
            </div>

            <div class="info-grid">
                <div class="info-card">
                    <h3><i class="fas fa-user"></i> Full Name</h3>
                    <div class="value">{{name}}</div>
                </div>
                <div class="info-card">
                    <h3><i class="fas fa-envelope"></i> Email</h3>
                    <div class="value">{{email}}</div>
                </div>
                <div class="info-card">
                    <h3><i class="fas fa-phone"></i> Phone</h3>
                    <div class="value">{{phone}}</div>
                </div>
                <div class="info-card">
                    <h3><i class="fas fa-building"></i> Company</h3>
                    <div class="value">{{company}}</div>
                </div>
            </div>

            <div class="section-title">
                <i class="fas fa-comment-dots"></i> Message
            </div>
            <div class="message-card">
                <p>{{message}}</p>
            </div>

            <div class="section-title">
                <i class="fas fa-microchip"></i> Technical Details
            </div>
            <div class="technical-section">
                <div class="tech-grid">
                    <div class="tech-item">
                        <div class="label">Submission ID:</div>
                        <div class="value">{{id}}</div>
                    </div>
                    <div class="tech-item">
                        <div class="label">IP Address:</div>
                        <div class="value">{{ip}}</div>
                    </div>
                    <div class="tech-item">
                        <div class="label">Location:</div>
                        <div class="value">{{country}} - {{city}} - {{region}} - {{metro}} - {{area}} - {{timezone}}</div>
                    </div>
                    <div class="tech-item">
                        <div class="label">Source:</div>
                        <div class="value">{{ref_website}}</div>
                    </div>
                    <div class="tech-item">
                        <div class="label">Timestamp:</div>
                        <div class="value">{{created_at}}</div>
                    </div>
                    <div class="tech-item">
                        <div class="label">User Agent:</div>
                        <div class="value">{{user_agent}}</div>
                    </div>
                </div>
            </div>

            <div class="section-title">
                <i class="fas fa-bug"></i> Processing Error
            </div>
            <div class="error-section">
                <div class="error-title">
                    <i class="fas fa-triangle-exclamation"></i> {{error.title}}
                </div>
                <p>{{error.description}}</p>
                <div class="error-content">
                    {{#each error.details}}
                    {{this}}<br />
                    {{/each}}
                </div>
            </div>

            <div class="action-buttons">
                <a href="{{actions.view_log_url}}" class="btn btn-primary">
                    <i class="fas fa-eye"></i> View Full Error Log
                </a>
                <a href="{{actions.view_lead_url}}" class="btn btn-secondary">
                    <i class="fas fa-user-circle"></i> View Lead Details
                </a>
                <a href="{{actions.retry_url}}" class="btn btn-error">
                    <i class="fas fa-sync-alt"></i> Retry Processing
                </a>
            </div>
        </div>

        <div class="email-footer">
            <p>You received this alert because you're a system administrator.</p>
            <div class="footer-links">
                <a href="{{panel_url.links.settings}}"><i class="fas fa-cog"></i> Notification Settings</a>
                <a href="{{panel_url.links.help}}"><i class="fas fa-question-circle"></i> Help Center</a>
                <a href="{{panel_url.links.docs}}"><i class="fas fa-book"></i> Documentation</a>
            </div>
            <div class="copyright">
                &copy; {{year}} {{company}}. All rights reserved.<br />
                This is an automated message - please do not reply directly
            </div>
        </div>
    </div>
</body>

</html>