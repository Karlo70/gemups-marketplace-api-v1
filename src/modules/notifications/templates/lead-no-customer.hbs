<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="x-apple-disable-message-reformatting" />
    <title>Thank You from {{name}}</title>
    <style>
      body, table, td, a {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
        text-size-adjust: 100%;
        margin: 0;
        padding: 0;
      }
      body {
        width: 100%;
        height: 100%;
        font-family: "Segoe UI", Roboto, Arial, sans-serif;
        background-color: #f9f9fb;
        color: #3A3E49;
        line-height: 1.6;
      }
      table {
        border-collapse: collapse !important;
        width: 100%;
      }
      .email-container {
        max-width: 600px;
        margin: 40px auto;
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        border: 1px solid #e5e7eb;
        overflow: hidden;
      }
      .email-header {
        background-color: #f9fafb;
        color: #7c3aed;
        padding: 30px 20px;
        text-align: center;
        border-bottom: 1px solid #e5e7eb;
      }
      .email-header img {
        width: 160px;
        margin-bottom: 14px;
      }
      .email-header h1 {
        margin: 0;
        font-size: 22px;
        font-weight: 600;
      }
      .email-body {
        padding: 30px;
        font-size: 16px;
        color: #4b5563;
      }
      .email-body h2 {
        color: #7c3aed;
        font-size: 20px;
        font-weight: 600;
        margin-bottom: 15px;
      }
      .email-body p {
        margin: 10px 0;
      }
      .email-body .info-block {
        background-color: #f3f4f6;
        padding: 15px;
        border-radius: 8px;
        margin: 20px 0;
        line-height: 1.5;
      }
      .info-label {
        font-weight: 600;
        color: #374151;
      }
      .email-footer {
        background-color: #f9fafb;
        text-align: center;
        padding: 20px;
        font-size: 14px;
        color: #6b7280;
        border-top: 1px solid #e5e7eb;
      }
      .email-footer a {
        color: #8b61ec;
        text-decoration: none;
      }

      @media screen and (max-width: 600px) {
        .email-body, .email-header, .email-footer {
          padding: 20px !important;
        }
      }
    </style>
  </head>
  <body>
    <center>
      <table role="presentation" class="email-container">
        <!-- Header -->
        <tr>
          <td class="email-header">
            <img src="{{backend_url}}/images/defaults/app-logo.png" alt="{{owner_company_name}} Logo" />
            <h1>Thank You for Contacting {{name}}</h1>
          </td>
        </tr>

        <!-- Body -->
        <tr>
          <td class="email-body">
            <h2>Hi {{name}},</h2>
            <p>Thank you for reaching out! We've received your message and our team will get back to you shortly.</p>

            <div class="info-block">
              <p><span class="info-label">Name:</span> {{name}}</p>
              <p><span class="info-label">Email:</span> {{email}}</p>
              <p><span class="info-label">Phone:</span> {{phone}}</p>
              {{#if company_name}}<p><span class="info-label">Company Name:</span> {{company_name}}</p>{{/if}}
              {{#unless (eq message "")}}<p><span class="info-label">Message:</span> {{message}}</p>{{/unless}}
            </div>

            <p>Meanwhile, if your message is urgent, feel free to reply to this email or contact us directly at our support line.</p>
            <p>We appreciate your interest in {{owner_company_name}}!</p>
          </td>
        </tr>

        <!-- Footer -->
        <tr>
          <td class="email-footer">
            &copy; {{year}} {{owner_company_name}}. All rights reserved.
          </td>
        </tr>
      </table>
    </center>
  </body>
</html>
